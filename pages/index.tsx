import React, { FC } from 'react';

import { Layout } from '../components/layout';

const HomePage: FC = () => {
    return (
        <Layout title="About">
            <h1>About service</h1>
        </Layout>
    );
};

export default HomePage;
